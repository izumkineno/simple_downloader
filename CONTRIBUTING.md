# 贡献指南

首先感谢你有兴趣为 simple_downloader 贡献代码！本文档将帮助你了解如何参与项目贡献。

## 行为准则

在参与贡献之前，请先阅读我们的 [Code of Conduct](CODE_OF_CONDUCT.md)，所有贡献者都需要遵守其中的规定。

## 如何贡献

### 1. 报告问题

如果你发现了 bug 或者有功能建议，请通过 GitHub Issues 报告：

#### 报告 Bug 时请提供：
- **Bug 描述**：清晰简洁地描述 bug 是什么
- **复现步骤**：详细的复现步骤，包括：
  - 使用的 simple_downloader 版本
  - 操作系统和版本
  - Rust 版本
  - 最小化的可复现代码
  - 实际发生的行为和预期的行为
- **错误日志**：完整的错误信息和堆栈跟踪
- **附加信息**：任何其他可以帮助定位问题的信息

#### 功能请求时请提供：
- **功能描述**：清晰地描述你想要的功能是什么
- **使用场景**：这个功能的使用场景是什么
- **预期行为**：你期望这个功能如何工作
- **替代方案**：你考虑过的其他替代方案

### 2. 贡献代码

我们欢迎所有形式的代码贡献，包括但不限于：
- 修复 Bug
- 实现新功能
- 性能优化
- 代码质量改进
- 文档完善
- 测试用例补充

#### 贡献流程：
1. **Fork 仓库**：点击项目主页右上角的 Fork 按钮
2. **克隆仓库**：`git clone https://github.com/your-username/simple_downloader.git`
3. **创建分支**：从 main 分支创建新的功能分支，分支命名建议：
   - `feature/xxx`：新功能
   - `fix/xxx`：修复 Bug
   - `docs/xxx`：文档改进
   - `perf/xxx`：性能优化
4. **提交代码**：在你的分支上进行开发
5. **提交 PR**：向原仓库的 main 分支提交 Pull Request
6. **代码审核**：维护者会审核你的代码，可能会提出修改意见
7. **合并代码**：审核通过后，代码会被合并到 main 分支

## 开发环境搭建

### 前置要求
- Rust 1.65 或更高版本
- Git
- 建议安装以下工具：
  - `cargo fmt`：代码格式化
  - `cargo clippy`：代码静态检查
  - `cargo tarpaulin`：测试覆盖率统计
  - `cargo doc`：文档生成

### 搭建步骤
```bash
# 1. 克隆仓库
git clone https://github.com/your-username/simple_downloader.git
cd simple_downloader

# 2. 构建项目
cargo build --all-features

# 3. 运行测试
cargo test --all-features

# 4. 运行示例
cargo run --example download
```

### 常用开发命令
```bash
# 格式化代码
cargo fmt --all

# 静态代码检查
cargo clippy --all-features --all-targets -- -D warnings

# 运行所有测试
cargo test --all-features --all-targets

# 生成并打开文档
cargo doc --all-features --open

# 检查测试覆盖率（需要安装 cargo-tarpaulin）
cargo tarpaulin --all-features --out Html
```

## 代码规范

### 1. 代码风格
- 严格遵循 Rust 官方代码风格，使用 `cargo fmt` 自动格式化
- 每行代码长度建议不超过 120 个字符
- 使用 4 空格缩进，不要使用 Tab
- 公共 API 必须包含完整的 rustdoc 文档
- 复杂的算法实现必须包含注释说明原理

### 2. 命名规范
- 类型名使用大驼峰命名法（UpperCamelCase）
- 函数、变量、宏名使用蛇形命名法（snake_case）
- 常量使用全大写下划线分隔（UPPER_SNAKE_CASE）
- 泛型参数使用短大写字母（T, U, V 等），复杂的使用描述性名称
- 避免使用缩写，除非是广泛使用的通用缩写

### 3. 注释规范
- 公共 API 必须有 rustdoc 文档注释（///）
- 文档注释应该包含：
  - 功能描述
  - 参数说明
  - 返回值说明
  - 错误说明（如果有）
  - 示例代码（对于重要的 API）
  - 安全注意事项（如果有）
- 非公开函数如果逻辑复杂也应该有注释说明
- 代码中的 "魔数" 必须有注释说明含义
- FIXME/TODO 注释必须包含作者和预计解决时间

### 4. 错误处理
- 使用 `thiserror` 定义自定义错误类型
- 公共 API 的错误必须清晰明确，方便用户处理
- 不要使用 `unwrap()`/`expect()` 除非你 100% 确定不会 panic
- 错误信息要清晰明了，说明发生了什么问题以及如何解决

### 5. 安全规范
- 所有用户输入必须进行验证
- 文件路径操作必须防止路径穿越攻击
- 处理敏感信息（如密码、认证信息）时要注意安全，不要打印或记录到日志中
- 网络请求必须验证 SSL 证书，默认不要禁用证书验证
- 任何可能导致内存安全的代码必须经过严格审核

## 提交规范

### 提交信息格式
提交信息遵循 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/v1.0.0/) 规范：

```
<类型>(<作用域>): <主题>

<正文>

<页脚>
```

#### 类型说明：
- `feat`：新增功能
- `fix`：修复 Bug
- `docs`：文档更新
- `style`：代码格式调整（不影响代码运行）
- `refactor`：代码重构（既不新增功能也不修复 Bug）
- `perf`：性能优化
- `test`：测试相关修改
- `ci`：CI/CD 相关修改
- `chore`：其他不修改源码或测试的修改

#### 示例：
```
feat(downloader): 添加下载速度限制功能

- 实现了基于令牌桶算法的速度限制
- 支持全局速度限制和单任务速度限制
- 添加了对应的配置选项和 API

Closes #123
```

### 提交要求：
- 每个提交应该是一个独立的逻辑单元，不要把多个不相关的修改放在一个提交中
- 提交主题简洁明了，不超过 50 个字符
- 正文详细说明修改的内容和原因，每行不超过 72 个字符
- 如果修复了某个 Issue，在页脚中加上 `Closes #issue-number`，会自动关闭对应的 Issue

## Pull Request 规范

### PR 标题
PR 标题同样遵循 Conventional Commits 规范，清晰说明 PR 的内容。

### PR 内容
PR 描述应该包含：
- **修改类型**：修复 Bug / 新增功能 / 文档改进 / 性能优化 等
- **修改内容**：详细描述做了哪些修改
- **修改原因**：为什么要做这些修改
- **测试情况**：如何验证修改是正确的，添加了哪些测试
- **影响范围**：这些修改会影响哪些功能模块

### 审核要求
- 所有 PR 必须至少有一名维护者审核通过才能合并
- CI 检查必须全部通过
  - 代码格式化检查（cargo fmt）
  - 静态检查（cargo clippy）
  - 所有测试通过（cargo test）
  - 文档生成成功（cargo doc）
- 新增功能必须包含对应的测试用例
- 涉及公共 API 修改的，必须更新对应的文档

### 代码审核要点
审核者会从以下方面审核代码：
- 功能正确性：是否实现了预期的功能，是否有 Bug
- 代码质量：是否符合代码规范，是否有优化空间
- 性能影响：是否会影响性能，是否有性能优化的可能
- 安全性：是否存在安全隐患
- 可维护性：代码是否容易理解和维护
- 文档完整性：公共 API 是否有完整的文档
- 测试覆盖：是否有足够的测试覆盖

## 测试指南

### 测试分类
项目包含三类测试：
1. **单元测试**：测试单个函数或模块的功能，放在对应模块的 `#[cfg(test)]` 块中
2. **集成测试**：测试多个模块之间的协作，放在 `tests/` 目录下
3. **示例测试**：测试示例代码的正确性，放在 `examples/` 目录下

### 编写测试的要求
- 所有新功能必须包含对应的测试用例
- Bug 修复必须添加回归测试，防止相同问题再次出现
- 测试用例应该覆盖正常场景、边界场景和错误场景
- 测试代码同样需要遵循代码规范
- 测试应该是幂等的，多次运行结果一致
- 不要在测试中依赖外部网络或其他不稳定的资源，如果需要使用 mock

### 运行测试
```bash
# 运行所有测试
cargo test --all-features

# 运行指定测试
cargo test --all-features test_name

# 运行集成测试
cargo test --all-features --test integration_test_name

# 运行示例测试
cargo run --example example_name
```

## 文档规范

### 文档类型
1. **API 文档**：使用 rustdoc 写在代码注释中
2. **指南文档**：放在 `docs/` 目录下，使用 Markdown 格式
3. **示例代码**：放在 `examples/` 目录下，必须可以正常运行
4. **README**：项目首页，包含快速开始和功能介绍

### 文档要求
- 中文文档使用简体中文，英文文档使用美式英语
- 技术术语保持统一，避免歧义
- 示例代码必须正确，可以直接运行
- 文档中的命令和配置必须经过验证
- 重大功能变更必须同步更新对应的文档

## 版本发布流程

项目维护者会定期发布新版本：
1. 更新 CHANGELOG.md，记录本次版本的变更内容
2. 更新 Cargo.toml 中的版本号
3. 打版本标签（格式：vX.Y.Z）
4. 发布到 crates.io
5. 在 GitHub Releases 中发布版本说明

## 贡献者许可协议

通过提交代码到本项目，你同意：
1. 你的贡献将在项目的 [LICENSE](LICENSE) 许可证下发布
2. 你有权授予上述许可
3. 你的贡献是原创作品
4. 如果你的贡献包含第三方代码，你已经获得了相应的授权，并注明了版权信息

## 社区交流

如果你有问题或想讨论，可以通过以下方式：
- GitHub Issues：提交 Bug 或功能请求
- GitHub Discussions：功能讨论、使用问题交流
- 邮件列表：如果有相关需求可以后续设立

## 常见问题

### Q: 我是新手，不知道从哪里开始贡献？
A: 可以先查看带有 "good first issue" 标签的 Issue，这些 Issue 比较适合新手。也可以从完善文档、补充测试用例开始。

### Q: 我的 PR 很久没有被审核怎么办？
A: 可以在 PR 中 @ 维护者，或者通过其他方式提醒我们。我们会尽力及时审核所有 PR。

### Q: 我想实现一个大的功能，应该怎么做？
A: 建议先开一个 Issue 讨论，说明你的设计方案，避免重复工作或者设计不符合项目方向导致 PR 被拒绝。

### Q: 贡献代码有什么奖励吗？
A: 所有贡献者都会在版本发布说明中被提及，特别重大的贡献会被加入项目贡献者列表。我们也会不定期给活跃贡献者发送小礼品。

再次感谢你的贡献！🎉
