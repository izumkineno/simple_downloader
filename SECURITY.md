# 安全政策

## 支持的版本

目前项目团队目前为以下版本提供安全更新支持：

| 版本 | 支持状态 | 支持结束时间 |
| ---- | -------- | ---------- |
| 0.1.x | ✅ 完全支持 | 待定 |
| < 0.1 | ❌ 不再支持 | - |

## 报告安全漏洞

我们非常重视项目的安全性。如果您发现了安全漏洞，请按照以下步骤报告：

### 报告方式

请通过电子邮件发送漏洞报告：
**邮箱**：security@example.com
**PGP 公钥**：可在此处获取 [公钥链接]（如果没有 PGP 加密的报告将优先处理。

### 报告内容
请在报告中包含以下信息：

1. **漏洞描述：漏洞类型（例如：缓冲区溢出、SQL 注入、证书验证绕过等）
2. **影响范围**：受影响的版本和功能模块
3. **复现步骤**：详细的漏洞复现步骤，包括：
   - 运行环境（操作系统、Rust 版本等）
   - 触发条件
   - 最小化的复现代码或 POC
4. **影响评估**：漏洞可能造成的影响，例如：
   - 远程代码执行
   - 本地权限提升
   - 信息泄露
   - 拒绝服务攻击
5. **建议的修复方案（如果有）

### 报告处理流程

1. **确认收到**：我们会在 24 小时内确认收到你的报告。
2. **初步评估**：在 3 个工作日内完成初步评估，确定漏洞的严重程度和影响范围。
3. **修复开发**：根据漏洞的严重程度，我们会尽快开发修复方案：
   - 严重漏洞：72 小时内提供修复方案
   - 中等漏洞：1 周内提供修复方案
   - 低危漏洞：1 个月内提供修复方案
4. **漏洞披露**：修复发布后，我们会在 1 周内公开披露漏洞详情，并致谢。

### 披露政策

### 安全公告会通过以下渠道发布安全公告：
- GitHub Releases 页面
- 项目官方博客
- 项目邮件列表

## 安全最佳实践

### 用户在使用 simple_downloader 时，建议遵循以下安全最佳实践：

### 1. 版本管理
- 始终使用最新的稳定版本
- 定期检查安全公告，及时更新到包含安全修复的版本
- 不要使用已经停止支持的旧版本

### 2. 下载安全
- 仅从可信的源下载文件，验证下载链接的安全性
- 对于敏感文件下载，建议使用 HTTPS 协议
- 避免从不受信任或未知的源下载文件

### 3. 配置安全
- 不要禁用 SSL 证书验证，除非你完全了解风险
- 谨慎使用 `danger_accept_invalid_certs` 选项
- 不要在代码中硬编码代理密码或其他敏感信息
- 使用环境变量或安全的配置管理方式存储敏感信息

### 4. 权限控制
- 以最小权限原则运行程序，避免使用 root/管理员权限运行
- 确保下载目录的权限设置合理，避免其他用户可以访问下载的文件
- 不要将下载的文件保存到系统敏感目录

### 5. 多源下载安全
- 仅添加可信的下载源，避免使用未知或不受信任的源
- 如果启用文件校验功能（如果可用），验证下载文件的完整性
- 定期检查下载源的可用性和安全性

### 6. 断点续传安全
- 断点续传的元数据文件包含下载进度信息，建议妥善保管
- 不要将元数据文件分享给其他人，避免泄露下载历史
- 对于敏感文件下载完成后，建议删除对应的元数据文件

## 安全功能说明

simple_downloader 内置了以下安全功能：

### 1. HTTPS 安全
- 默认启用 SSL 证书验证
- 支持 TLS 1.2+ 协议，禁用不安全的 SSL 3.0、TLS 1.0、TLS 1.1 协议
- 支持证书钉住（Certificate Pinning）配置（通过自定义客户端实现）

### 2. 文件系统安全
- 安全的文件写入方式，避免部分写入的文件被误认为完整文件
- 下载完成前使用临时文件名，下载成功后再重命名为目标文件名
- 自动校验下载文件的大小与服务器返回的 Content-Length 是否一致

### 3. 输入验证
- 对下载 URL 进行严格的验证，避免路径遍历攻击
- 对文件路径进行规范化处理，防止路径穿越漏洞
- 对 HTTP 响应头进行安全解析，避免响应头注入攻击

### 4. 代理安全
- 代理认证信息不会被记录到日志中
- 支持 SOCKS5 代理的用户名密码认证
- 代理连接使用加密传输

## 已知安全限制

以下是已知的安全限制，使用时需要注意：

1. **断点续传元数据**：元数据文件默认没有加密，如果下载的是敏感文件，建议自行加密元数据文件或存储在安全的位置。
2. **下载文件校验**：当前版本不会自动校验下载文件的哈希值，建议用户自行校验下载文件的完整性。
3. **多源下载安全**：多源下载时，来自不同源的文件块不会进行额外的校验，建议仅使用可信的下载源。

## 漏洞奖励计划

我们目前没有正式的漏洞奖励计划，但对于负责任地报告严重漏洞的研究者，我们会：
- 在安全公告中公开致谢
- 邀请成为项目贡献者
- 提供项目相关的纪念品

## 致谢

感谢以下安全研究者对项目安全做出的贡献：

- 暂无

---

## 联系我们

如果您有任何安全相关的问题或建议，欢迎通过上述邮箱联系我们。
